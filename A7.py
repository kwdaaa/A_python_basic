# 2, 4, 6, 8 という4つの要素をもつ変数「even_numbers」というリストを定義。
even_numbers = [2, 4, 6, 8]

# 変数「even_numbers」の中身を順番に取り出して、中身を変数「number」に定義。
for number in even_numbers:
    # 変数「number」に２をかけたものを出力。
    print(number * 2)
